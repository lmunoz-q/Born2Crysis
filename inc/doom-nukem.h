#ifndef DOOM_NUKEM_H
# define DOOM_NUKEM_H

#include <libui.h>
#include <SDL2/SDL.h>

typedef struct	s_e
{
	t_libui_window	*win;
	SDL_Thread		*thread[3];
}				t_e;

typedef struct	s_player
{

}				t_player;

#endif
